<Page x:Class="LandLyst.Reservationer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LandLyst"
      mc:Ignorable="d" 
      d:DesignHeight="379" d:DesignWidth="800"
      Title="Reservationer">

    <DataGrid x:Name="reservationer">
        <!--<DataGrid.Columns>
            <DataGridTextColumn Binding="{Binding reservation_id}" Header="Rum id" x:Name="resid" />
            <DataGridTextColumn Binding="{Binding services}" Width="400" Header="Tillægsydelser" x:Name="roomid"/>
            <DataGridTextColumn Binding="{Binding priceday}" Header="Pris pr. nat" x:Name="email"/>
            <DataGridTextColumn Binding="{Binding totalprice}" Header="Total pris" x:Name="cInDate"/>
            <DataGridTextColumn Binding="{Binding totalprice}" Header="Total pris" x:Name="cOutDate"/>
            <DataGridTemplateColumn >
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Button x:Name="seebtn" Content="Se værelse" Click="Seebtn_Click"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>-->
    </DataGrid>

</Page>
