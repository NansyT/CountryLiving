<Page x:Class="LandLyst.StartSide"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LandLyst"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StartSide">

    <DockPanel x:Name="DPVV" >
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Height="40">
            <Rectangle Width="20"/>
            <Button x:Name="Opretbtn" Content="Opret reservation" Padding="5" Height="30" />
            <Rectangle Width="20"/>
            <Button x:Name="Ventendebtn" Content="Ventende reservation" Padding="5" Height="30" />
        </StackPanel>
        <DataGrid>
            <!-- Alle reserveringer db her-->
        </DataGrid>
    </DockPanel>
</Page>
